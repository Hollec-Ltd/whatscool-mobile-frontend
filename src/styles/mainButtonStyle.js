import { StyleSheet } from 'react-native';

import { DARK_BLUE_COLOR, GREY_COLOR } from './colors';

const mainButtonStyles = StyleSheet.create({
  buttonHolder: {
    paddingVertical: 12,
    paddingHorizontal: 30,
    borderRadius: 30,
  },
  buttonText: {

  },
});

export default mainButtonStyles;
